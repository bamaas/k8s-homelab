# Base

The `base` folder contains the foundational Kubernetes configurations that are shared across all environments.
These configurations define the core applications and resources that form the baseline deployment.

## Folder Structure

- **`apps/`**: Application deployments
- **`namespaces/`**: Namespace bound besources that are not directly applicable to an application
