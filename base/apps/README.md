# Apps

The `apps` folder contains application deployments using Helm charts and Kustomize configurations.

## Folder Structure

- **`<app>`**: Directory for each application (e.g., `sonarqube/`, `sigrid/`), containing its deployment manifests and configuration files.
