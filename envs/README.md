# Environments

The `envs` folder contains environment specific configurations that customize the [base configuration](../base/README.md) for different environments.

## Folder Structure

- **`dev/`**: Development environment configurations.
- **`acc/`**: Acceptance environment configurations.
- **`prd/`**: Production environment configurations.
